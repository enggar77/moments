export default function Loading() {
	return (
		<div className="absolute inset-0 flex items-center justify-center bg-base-100 backdrop-blur-sm">
			<span className="loading loading-dots loading-xl"></span>
		</div>
	);
}
